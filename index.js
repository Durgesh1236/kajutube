
// Function to play the video when clicked
function playVideo(videoId) {
    var video = document.getElementById(videoId);
    video.play();
}

// Function to pause the video when clicked
function pauseVideo(videoId) {
    var video = document.getElementById(videoId);
    video.pause();
}
